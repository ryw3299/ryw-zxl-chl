# ⚠️  DEPRECATED — uv + pyproject.toml is the source of truth.
#
# This file is kept only as a fallback for CI / pipx / Docker contexts that
# don't have uv yet.  To regenerate it from the canonical lockfile:
#
#     uv export --no-hashes --format requirements-txt > requirements.txt
#
# For all normal workflows use:
#     uv sync              # dev install
#     uv sync --no-dev     # production install
#
# See pyproject.toml for the actual dependency declaration.

pydantic>=2,<3
pydantic-settings>=2,<3
PyYAML>=6,<7
PyMuPDF>=1.27,<2
python-pptx>=1.0,<2
python-docx>=1.2,<2
pytest>=8,<9
pywin32>=306; platform_system == "Windows"
fastapi>=0.115,<1
uvicorn[standard]>=0.34,<1
sqlalchemy>=2,<3
pymysql>=1,<2
cryptography>=43,<46
python-multipart>=0.0.18
python-dotenv>=1,<2
httpx>=0.27,<1
numpy>=1.24
faiss-cpu>=1.8
jieba>=0.42
rank-bm25>=0.2
cachetools>=5
openhands>=0.4,<5
edge-tts>=6,<8
